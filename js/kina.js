
var desc = "gratis men du er produktet"

kinaPrice = function (checkoutTime, checkinTime) {
return 0;
}

document.addEventListener('DOMContentLoaded', () => {
    let parkingLot = new parkingLot(desc);
    new moniter(parkingLot);
    main(parkingLot);
});